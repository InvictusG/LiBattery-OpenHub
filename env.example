# Environment variables for LiBattery OpenHub

# 1. MongoDB Connection String (Optional)
# If you want to use a database for caching or storing metadata, provide your MongoDB connection string here.
# For demo purposes or if you prefer fetching data directly from GitHub all the time, this can be left empty.
MONGODB_URI=

# 2. GitHub Personal Access Token (Required)
# This token is required to fetch data from the GitHub API. 
# It increases the rate limit and allows access to repository details.
#
# How to create a token:
# 1. Go to your GitHub Settings -> Developer settings -> Personal access tokens -> Tokens (classic).
# 2. Click "Generate new token" (or "Generate new token (classic)").
# 3. Give it a name (e.g., "LiBatteryOpenHub").
# 4. Select the "public_repo" scope. This is sufficient for reading public repository data.
# 5. Click "Generate token" and copy the token here.
GITHUB_TOKEN=

# JWT 密钥 (用于用户认证)
JWT_SECRET=your_jwt_secret_key_here

# 环境类型
NODE_ENV=development

# 应用基础 URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# API 基础 URL
NEXT_PUBLIC_API_URL=http://localhost:3000/api 